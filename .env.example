# Server-only provider URLs (do not expose as NEXT_PUBLIC_*).
ADVICE_PROVIDER_PRIMARY_URL=https://api.adviceslip.com/advice
ADVICE_PROVIDER_SECONDARY_URL=https://zenquotes.io/api/random

# Provider fetch timeout in milliseconds.
ADVICE_REQUEST_TIMEOUT_MS=3200
