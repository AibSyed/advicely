# Server-only provider URLs. Do not expose as NEXT_PUBLIC_*.
ADVICE_SLIP_URL=https://api.adviceslip.com/advice
ZEN_QUOTES_RANDOM_URL=https://zenquotes.io/api/random

# Provider fetch timeout in milliseconds.
DRAW_REQUEST_TIMEOUT_MS=3200
